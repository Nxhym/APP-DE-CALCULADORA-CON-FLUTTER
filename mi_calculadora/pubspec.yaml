name: mi_calculadora
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # CAMBIO IMPORTANTE: Usar la versión 2.x para compatibilidad con StateNotifier
  flutter_riverpod: ^2.6.1
  go_router: ^14.0.0 # Recomiendo bajar go_router también para evitar conflictos, aunque la 17 podría funcionar
  shared_preferences: ^2.2.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Versiones más estables para evitar alertas de analyzer